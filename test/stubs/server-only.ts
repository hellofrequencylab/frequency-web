// Empty stub for the `server-only` Next.js guard under vitest (it has no runtime
// package). Aliased in vitest.config.ts so server-only modules can be unit-tested for
// their pure exports without the real build-time guard.
export {}
