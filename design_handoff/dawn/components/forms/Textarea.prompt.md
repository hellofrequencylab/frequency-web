A multi-line text field matching Input's chrome — used by the feed composer, replies, and bios.

```jsx
<Textarea label="What's happening near you?" rows={4} placeholder="Share a moment from this week…" />
```

- Passes through all native `<textarea>` props; `invalid` + `hint` work like Input.
