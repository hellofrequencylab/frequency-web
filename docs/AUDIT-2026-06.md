# Site audit — 2026-06-18

> **Bottom line:** the app was healthy, not broken. The "laggy" feel traced to ~20 serial
> database round-trips in the authenticated shell that runs on *every* page, plus the feed's
> heaviest query blocking its own shell. Both are fixed. No exploitable security issues were
> found (two hardening gaps closed). SEO/AIO was strong; additive schema gaps closed. 14 dead
> files + one orphaned per-user cluster removed. All changes ship green: **tsc 0 · lint 0 ·
> 1044 tests · build ✅**.

Method: six read-only audit agents (performance, DB query-shape, SEO/AIO, security ×3, orphaned
code) plus the Supabase performance + security advisors, synthesized here. Fixes were applied by
the orchestrator and validated together.

Legend: ✅ done · ⏳ recommended follow-up · ⚠️ noted, intentional · 🔴 was broken

---

## 1. Performance — the "laggy" fix

| # | Issue | Fix | Status |
|---|---|---|---|
| 1 | `app/(main)/layout.tsx` ran ~16 independent reads **serially** before the shell could paint — on every authed navigation | Collapsed into **one `Promise.all` wave** (view-as, unread count, permissions, menu config, viewer hats, help index, consent, demo flags, chrome overrides, Space, chores, staff). Ordering-dependent guards + theme chain kept after it. | ✅ |
| 2 | `/feed` ran its heaviest query (`FeedList`) **outside Suspense**, blocking the greeting/hero; plus 5 serial awaits and a **duplicate profile read** | `FeedList` now streams behind `<Suspense>` with a skeleton; the 5 reads batched into one `Promise.all`; geo + membership reads parallelized (duplicate read removed) | ✅ |
| 3 | `/circles/[slug]` resolved profile, paid-viewer, capabilities, Insight access in series | One `Promise.all` (profile/paid only for a signed-in viewer) | ✅ |
| 4 | `/journeys/[slug]/learn` resolved cohort progress, kickoff event, phase events in series after the run loaded | One `Promise.all` | ✅ |
| 5 | Raw `<img>` in two hot paths (market grid thumbnails, channel cover band) bypassed `next/image` | Converted to `next/image` (`fill` + `sizes`); responsive AVIF/WebP | ✅ |
| — | `public/images/site/` flagged as "4.7 MB shipped raw" | **Verified false alarm:** every one is rendered through `SiteImage`/`next/image`, so visitors already get optimized AVIF/WebP — the raw bytes never ship. No change. | ⚠️ |
| — | `lib/traits/segments.ts` unbounded member scans; `lib/attribution/backfill.ts` N+1 write loop | Cold/admin paths, not the user-facing lag. Left as-is; see Follow-ups. | ⏳ |

**Brand mark (`components/layout/brand-mark.tsx`)** intentionally left on raw `<img>`: a single small
operator logo with intrinsic `w-auto` sizing (unknown aspect ratio) is a poor `next/image` fit for
negligible gain.

## 2. Database — advisors (migration `20260704000000_audit_perf_indexes.sql`)

| # | Issue | Fix | Status |
|---|---|---|---|
| 1 | **18 unindexed foreign keys** — deleting a parent row (esp. a `profiles` row referenced by `host_id` / `created_by` / `updated_by` / `owner_profile_id`) seq-scanned each child table | Added 18 covering indexes. Verified the advisor was right (existing composite indexes like `{profile_id, plan_id}` don't cover a plan-only FK check — the FK column wasn't leading) | ✅ |
| 2 | `journey_completions` RLS re-evaluated `auth.uid()` **per row** (`auth_rls_initplan`) | Wrapped as `(select auth.uid())` — evaluated once per query. Identical predicate | ✅ |

Auth-config advisors (anonymous sign-ins, leaked-password protection) are **dashboard toggles**,
left for the owner — see Follow-ups.

## 3. Security — no exploitable issues; two hardening gaps closed

No 🔴 findings across server-action authz, the economy guard chain, the QR/SVG render path, the
SSRF guard, open-redirects, or the secret surface. The "service-role bypasses RLS → re-check authz
in app code" discipline holds everywhere; `SUPABASE_SERVICE_ROLE_KEY` is never imported by a client
file; QR styles are sanitized on **both** write and read.

| # | Hardening item | Fix | Status |
|---|---|---|---|
| 1 | `requireMarketer` admitted **any** `team_members` staff row to mutate campaigns/variants/nurture (a read-only analyst could write) | Now requires `staffCan(role, 'marketing', 'write')` in all three marketing action files | ✅ |
| 2 | `endSeasonAction` + `admin/menu` gated on the **deprecated `community_role`** ladder, not the ADR-208 `web_role` staff axis | Switched to `isJanitor(caller.webRole)`. Verified both current janitors carry `web_role = janitor`, so **zero behavior change today** — purely a correctness fix | ✅ |
| 3 | Host-tier badge grant/revoke + store/CRM are host-gated without a staff-domain check | Confirmed they don't move the economy (badge state only); documented as intentional steward tools | ⚠️ |
| 4 | `canReceivePayouts` exported from a `'use server'` file | Low-value (payout-eligibility boolean), called internally with own id; de-export later | ⏳ |

## 4. SEO / AIO — strong foundation; additive schema gaps closed

Crawl plumbing, `metadataBase`, the JSON-LD library, and per-page metadata were all in place; the
recently-changed surfaces (Practices, Journeys discovery, On Air, help) were **intact, not
regressed**. Closed the coverage gaps, all additive (reused existing `breadcrumbSchema` / `faqSchema`
/ `circleListSchema` + `<JsonLd>`):

- ✅ Breadcrumb JSON-LD on help index / category / changelog (only the article page had it)
- ✅ Twitter cards on circle + topic discover routes; explicit `card` on the discover landing
- ✅ Minimal entity JSON-LD on the circle detail page
- ✅ Breadcrumb JSON-LD on the 6 marketing pillar pages; **FAQPage** on `/pricing` (from the on-page Q&A verbatim)

## 5. Orphaned code — cleaned up

**14 whole-dead files removed** (every export unverified-unused, each confirmed by grep incl. dynamic
imports + registries): `components/ui/page-header.tsx`, `components/theme/theme-provider.tsx`, the
admin nav trio (`admin-top-nav`, `admin-left-nav`, `admin-profile-card` — superseded by `AdminTopMenu`),
the four `components/admin/growth/*-tab.tsx` (superseded by the inline ADR-264 rebuild),
`components/admin/modules/qr-generator-module.tsx`, `components/quest/journey-rank.tsx`,
`app/(main)/admin/create-menu.tsx`, `app/(main)/crew/season-banner.tsx`, `lib/ai/journey-outline.ts`.

Plus the **per-user Practices layout cluster** (`practices-layout-editor.tsx`, `practices/layout-actions.ts`,
`lib/practices-page-config.ts`) — removed as part of reverting Practices to the standard module-layout
pattern (§6).

**Confirmed false-positives (kept):** `admin/qr/scan-map.tsx` (dynamic-imported), `lib/comms/sms.ts`
(intentional groundwork, ADR-256), `components/page-editor/blocks/*` (used in Puck registries),
`lib/database.types.ts` `Constants` (generated).

**Should-be-wired (built, one wire short — left for a product call):** `createJourneyFromTemplateAction`
(the Master Journey Template path, ADR-307 — has no UI yet), `uncompleteJourneyLessonAction`,
`enrollInRun` / `listRunsForCircle`, `recordConsent` (no consent-write UI), `clearPageSeo`,
`getMemberTags`. See Follow-ups.

## 6. Practices page — editor corrected

The #896 redesign had introduced a **bespoke per-user "Customize this page" block** and removed the
standard operator **Settings ▾ → Page → Layout** control. Reverted to the house pattern every other
module-driven page uses (`/lead`, `/crew`, `/journeys`): the page body renders `<PageModules
route="/practices">` and operators arrange it through the standard Settings dropdown. The inline
editor and its per-user cluster were removed (§5).

## 7. Personal QR codes — profile pic + standard format

Every member's personal `connect` code now **centers their current profile pic** (rounded buffer) and
uses the **standard format** (connected modules, rounded eyes/pupils — DEFAULT_STYLE):

- New helper `withMemberAvatar(style, avatarUrl)` layers the avatar on **at render time** (so a new
  photo never needs a reprint; the stored style is never polluted with a soon-stale URL).
- Applied across all four render surfaces: `/codes` (view), `/api/qr` (PNG/SVG download), `/print/qr`,
  and the admin QR studio.
- New codes mint in the standard format; migration `20260704010000_personal_codes_standard_format.sql`
  **re-output all 12 existing codes** to it (11 were on the legacy `sunset` preset).

---

## Validation

`npx tsc --noEmit` → 0 errors · `pnpm lint` → 0 · `pnpm test` → 1044/1044 · `pnpm build` → ✅

## Follow-ups (recommended, not blocking)

| Area | Item | Why deferred |
|---|---|---|
| Auth config | Turn off anonymous sign-ins; enable leaked-password protection | Dashboard toggles, owner's call |
| Perf (scale) | Bound `lib/traits/segments.ts` member scans; batch the `lib/attribution/backfill.ts` N+1 | Cold/admin paths; touch data-volume/write paths — want a test pass |
| Wiring | `createJourneyFromTemplateAction` (ADR-307 template path), `recordConsent` UI, run enroll/list helpers | Each needs a UI/product decision, not an audit fix |
| Security | De-export `canReceivePayouts` | Cosmetic |
| SEO | `faq` frontmatter so help articles can emit FAQPage | No article carries Q&A yet |
