# Full-site survey — 2026-06-25

> A read-only fan-out audit of the entire `frequency-web` surface (marketing, member
> app, admin suite, API routes, lib domain, DB/migrations, Resonance, AI/Vera, security,
> performance, SEO/AIO), followed by adversarial verification of every high-severity
> finding and the in-repo repairs. Companion to [`OPEN-THREADS.md`](OPEN-THREADS.md)
> (live tracker) and [`AUDIT-2026-06-15.md`](AUDIT-2026-06-15.md) (prior pass).

## Verdict

**The site is structurally healthy. The baseline gate is green — `tsc`, `eslint`,
`vitest` (2,284 tests), and `check:authz` all pass — so every defect found is the class
a green gate cannot see.** Twelve parallel slice audits surfaced **8 confirmed HIGH
defects** (0 refuted on verification), **10 medium**, and **15 low**. The strongest
cluster was **authorization on server actions + DB grants/predicates**. The in-flight
Pages-module PR area (`app/(main)/pages/**`, widgets, settings-drawer, page-chrome) had
**zero findings** and was left untouched.

Legend: ✅ fixed this pass · ⏳ owner-gated / deferred · 🔴 needs a decision

## Confirmed HIGH defects (adversarially verified)

| # | Status | Defect | Where | Resolution |
|---|--------|--------|-------|------------|
| 1 | ✅ | **Member Vera `confirmProposal` could dispatch operator-only tools** (mint Gems, mutate any CRM contact, draft outbound email) — it forwarded an arbitrary client `tool` to `executeConfirmedTool`, which trusts the caller as an authorized operator on arbitrary ids. | `app/onboarding/vera-actions.ts` · `lib/ai/vera/execute.ts` | Allow-listed the member-safe self-scoped tools (`remember_fact`, `set_profile_field`, `draft_intro`) before dispatch. |
| 2 | ✅ | **`deactivateMember` gated on community `host`, not staff `janitor`** — a single-circle host could disable ANY account platform-wide (ADR-127 = janitor-only). | `app/(main)/admin/actions.ts` | Swapped to `isJanitor(caller.webRole)`, matching every sibling. |
| 3 | ✅ applied | **`dashboard_health_summary` / `dashboard_space_health_summary` IDOR** — SECURITY DEFINER analytics RPCs EXECUTE-granted to `authenticated`; the space variant takes an arbitrary `_space_id`, reachable via PostgREST. | `supabase/migrations/20260801000000_engagement_scores.sql` | Migration `20260818000100` revokes the grant (app calls via service role). **Owner must apply to prod** — the live grant stands until then (mirrors ADR-371). |
| 4 | ✅ applied | **`is_space_member` / `is_space_admin` compared `profiles.id` FKs to `auth.uid()`** (the auth-user id), so every Space DB gate was always false. Defense-in-depth only (app reads via service role), but a broken wall. | `supabase/migrations/20260711080000…` · `…060000…` | Migration `20260818000000` resolves via `get_my_profile_id()`; also fixed the identical inline bug in `space_members_read`. **Owner applies to prod.** |
| 5 | ✅ built | **Resonance Engine has no member entry point** — `setMatchingConsent` / `setTargetOptOut` / `recordMatchOptIn` have zero callers, so `opted_in` is always false and every surface reads "No matches yet" forever. `roommates/page.tsx` even tells members to "turn on resonance matching in your settings" — a control that doesn't exist. | `lib/resonance/matches.ts` · `edges.ts` · `candidates.ts` | **Decision needed** — see Owner-gated #3. Wiring it activates a member consent surface + outbound intros; confirm launch scope before building. |
| 6 | ✅ | **Broadcast/dispatch editor lost the body on Edit** — `body` seeded from `''` and was never fetched; the `required` textarea opened blank, Save stayed disabled, and a save overwrote the original. | `app/(main)/admin/dispatches/*` | Fetch + seed `body` (`updateDispatch` already read/wrote it). |
| 7 | ✅ | **`/network` ran an unbounded `profiles` select** and rendered the whole community with client-side filtering. | `app/(main)/network/page.tsx` | Capped at 500 (mirrors `/circles`). Pagination + "showing first N" is the follow-up. |
| 8 | ✅ built | **Spaces SEO is built but unreachable by crawlers** — `(main)/layout.tsx` 307-redirects anon users to `/` on every `(main)` route, including networked `/spaces/<slug>`, while `sitemap.ts` advertises those URLs (with OG images + JSON-LD). The sitemap advertises URLs that redirect. | `app/(main)/layout.tsx` · `app/(main)/spaces/[slug]/layout.tsx` · `app/sitemap.ts` | **Decision needed** — see Owner-gated #2. (Fixed the breadcrumb JSON-LD parent to canonical `/spaces` as a down-payment.) |

## Medium / low — fixed this pass

| Status | Item | Where |
|--------|------|-------|
| ✅ | Cron auth used non-constant-time `!==` for the bearer secret | `lib/cron-auth.ts` |
| ✅ | `listings_update` RLS had no `WITH CHECK` (owner could reassign a listing) | migration `20260818000200` |
| ✅ | `set_profile_field` advertised `neighborhood` but the executor rejected it | `lib/ai/vera/execute.ts` |
| ✅ | Marketing CTAs (incl. primary "Host your first Circle") linked to retired `/how-it-works` & `/build` stubs, adding a redirect hop | `app/(marketing)/beta/*`, `how-to-build-community/*` |
| ✅ | `/how-it-works` used `redirect()` (307) not `permanentRedirect()` (308) | `app/(marketing)/how-it-works/page.tsx` |
| ✅ | Discover circle detail meta description not length-capped (~155) | `app/discover/circles/[id]/page.tsx` |
| ✅ | Space breadcrumb JSON-LD named in-app `/spaces/directory` as parent | `app/(main)/spaces/[slug]/layout.tsx` |
| ✅ | Circle detail blocked first paint on `FeedList` (now streamed behind `<Suspense>`) | `app/(main)/circles/[slug]/page.tsx` |
| ✅ | Docs drift: `ARCHITECTURE.md` claimed "no test framework" (there are 2,284 vitest tests) | `docs/ARCHITECTURE.md` |

## Medium / low — deferred backlog, now cleared (2026-06-25, systematic pass)

The full deferred backlog was designed as 10 blueprints by a parallel agent fan-out, then
applied in themed, individually-validated batches (`tsc` · `eslint` · `vitest` ·
`check:authz` green per batch). Only two genuinely owner-gated items remain (bottom rows).

| Status | Item | Resolution |
|--------|------|-----------|
| ✅ | Library / member practice cards linked to `/practices` (or `/practices/<id>`), not a slug | `getRankedPractice` now resolves slug→id against the table before reading the slug-less ranked view; every member-facing surface links `/practices/<slug>` (id fallback) and the detail page emits a slug canonical. |
| ✅ | QR stats loaded `qr_scans` unbounded | New bounded `qr_stats_summary(p_days)` RPC (group-by, service_role-only) + a pure `scanSummaryFromRpc` mapper; identical numbers, no full-table scan. **Migration `20260819000100` to apply to prod.** |
| ✅ | `/circles` index ran 6 serial independent awaits | All six overlap in one `Promise.all`; demo gating stays before the circles query and the auth chain stays serial within its closure, so output is byte-for-byte identical. |
| ✅ | Moderation queue N+1 prior-report count | Replaced the per-row count loop with one grouped fetch + a `Map` tally (Batch 1). |
| ✅ | Event / circle detail pages exported no `generateMetadata` | Both now emit title/description + OG cover + twitter cards via an anon admin-client resolve (Batch 1). |
| ✅ | `send_intro_email` sanitizer regex stripped spaces/punctuation | Fixed to contiguous control-char ranges `[\x00-\x1f\x7f]`, matching `sendPlaybookEmail`; CodeQL-clean. |
| ✅ | `embedPerson` never called (Resonance precision half) | New `refreshResonanceEmbeddings()` seeds one embedding per opted-in member nightly in the refresh-traits cron; gated on `resonanceLayerWritable()` + `aiAvailable()` (not `embeddingLayerAvailable()`, which would deadlock cold-start). |
| ✅ | CRM graph `PartyLink` ignored `contactId`; "Message {name}" linked to static `/messages`; dead reader exports | `PartyLink` → `/admin/marketing/contacts/<id>`; Message → `/people/<handle>`; dead exports removed (Batch 1). |
| ✅ | Space campaign sends didn't enforce `canEmailContact` | Send loop now gates each recipient on `canEmailContact(email,'marketing',spaceId)` (composes suppression + consent_state); unknown/unsubscribed are skipped, fail-closed. Owner decision: **enforce double-opt-in**. |
| ✅ | Commerce checkout never decremented `stock` | New atomic `decrement_commerce_stock_atomic(_order)` RPC wired into the paid settle (idempotent, raises `out_of_stock`); owner decision: **enforce inventory**. **Migration `20260819000000` to apply to prod.** |
| ✅ | `/network` ran its independent reads in series (mobile TTFB) | Header / viewer / settings / steward / 500-row directory kicked off together and awaited at point of use, so the directory fetch overlaps the `membersNear` RPC. Ordering tail unchanged. |
| ⏳ | Caller-row double-fetch dedup | Touches the shared, `cache()`-wrapped auth boundary used app-wide for a single-row-SELECT upside. Owner call; held. |
| ⏳ | CSP `connect-src` omits the GA4 regional collect endpoint | Dormant until GA is configured. |

## Owner-gated (config / decision / deploy — not closable by code alone)

1. ✅ **DONE — the 3 security migrations were applied to prod (2026-06-25) via Supabase MCP
   and verified** (`lock_down_dashboard_rpcs`, `fix_space_gate_profile_id`,
   `listings_update_with_check`). The dashboard-RPC IDOR is closed (advisor cleared); the
   app reads via the service role so behavior is unchanged.
2. ✅ **Spaces-SEO (#8):** decided **public header + footer** — networked `/spaces/<slug>`
   made anon-crawlable; the sitemap now advertises reachable URLs.
3. ✅ **Resonance launch (#5):** decided **full launch (consent + intro-accept)** — the opt-in
   surface is wired, the `send_intro_email` sanitizer fixed, and the `embedPerson` nightly cron
   (`refreshResonanceEmbeddings`) now seeds the precision layer.
4. ✅ **Commerce stock policy:** decided **enforce** — `decrement_commerce_stock_atomic` RPC
   wired into the paid settle (idempotent, no oversell).
5. ✅ **Space campaign consent policy:** decided **enforce double-opt-in** —
   `canEmailContact` gates every campaign recipient.

### ⚠️ Two migrations still to apply to prod (Supabase MCP — repo migrations do not touch the live DB)

| Migration | What it adds | Apply |
|-----------|--------------|-------|
| `20260819000000_commerce_stock_enforce.sql` | `decrement_commerce_stock_atomic(uuid)` RPC, service_role only | ⏳ apply + record |
| `20260819000100_qr_stats_summary_rpc.sql` | `qr_stats_summary(integer)` RPC, service_role only | ⏳ apply + record |

Both are additive + idempotent (`create or replace`) and locked down per ADR-371. The app
degrades fail-safe until applied (commerce stock simply isn't decremented; the QR stats page
reads zeros), so there is no broken state pre-apply — but the features are inert until then.

## Mobile + UI/UX + cleanup sweep (second pass, 2026-06-25)

A second 8-slice fan-out targeted the reported **mobile lag on `/network`**, plus
app-wide perf, responsive UI, connections/wiring, DAWN token compliance, dead code, and
accessibility. 7 confirmed, 1 refuted.

**`/network` mobile-lag root cause:** the grid rendered **every** member card (up to the
500 cap) with no windowing — hundreds of card subtrees are what stutter scroll on phones.
Fixed by rendering a 48-card slice with a "showing first N" notice (fetch stays capped at
500). The "add a grid skeleton" idea was **refuted** — Next 16's `loading.tsx` already
wraps the segment in a Suspense boundary and streams a skeleton on hard load.

| Status | Item | Where |
|--------|------|-------|
| ✅ | `/network` renders only the first 48 cards (DOM ~10x lighter) | `app/(main)/network/page.tsx` |
| ✅ | Directory + sidebar avatars get exact `sizes` (stop over-fetch) | `contact-card.tsx`, `community-sidebar.tsx` |
| ✅ | RSVP bottom bar no longer stacks on the mobile tab bar (safe-area offset) | `events/[slug]/rsvp-bottom-bar.tsx` |
| ✅ | Feed Create dead links re-pointed (New Conversation→/network, New Room→/messages, New Circle→/circles/new) | `components/feed/create-actions.ts` |
| ✅ | RSVP +1 inputs keyed by stable id, not array index | `components/events/rsvp-controls.tsx` |
| ✅ | Circle-host "New announcement" deep link now opens + pre-scopes the composer | `broadcast/page.tsx`, `broadcast-compose.tsx` |
| ✅ | New Event `?circle=` deep link honored (validated to the caller's own circles) | `events/new/page.tsx`, `event-form.tsx` |
| ✅ | `/outreach` orphan now linked from the Leadership dashboard (host+ gated) | `components/widgets/lead/lead-tools.tsx` |
| ✓ | `maplibre-gl` maps already `next/dynamic`-imported (code-split) — no change needed | `circles/discover/events/connections/qr` map clients |

**Deferred (needs-decision / owner sign-off):**
- ✅ **`/network` query parallelization (done 2026-06-25)** — the page header, viewer profile,
  connection settings, steward id, and the 500-row directory fetch now kick off together and are
  awaited at point of use, so the directory fetch is in flight during the `membersNear` RPC. The
  proximity-ordering chain consumes the same resolved `viewer` value and the sort is untouched, so
  nearby-first ordering is byte-for-byte unchanged by construction (a TTFB trim only). Runtime
  spot-check of nearby ordering on a deploy is still advised, but no read or sort input changed.
- ⏳ **Caller-row double-fetch dedup** — collapsing the duplicate caller read touches the shared,
  `cache()`-wrapped auth boundary used app-wide for a single-row-SELECT upside. Owner call; held.

## API-routes security re-run (closing the pass-1 gap)

A focused audit re-covered all **44** `app/**/route.ts` handlers. The surface is strong:
all 17 cron routes call `rejectUnauthorizedCron` first (fail-closed); all 4 webhooks
(Stripe ×2, Resend, Twilio) verify provider signatures with constant-time compares plus
replay/idempotency defenses; every `createAdminClient()` handler self-guards (ownership/role
check, credential-in-path token, or SECURITY DEFINER RPC); search routes escape LIKE
wildcards + cap length; the auth callback blocks protocol-relative open redirects; the OAuth
callback verifies a member-bound signed state. The `/q/[slug]` QR redirect to an owner-set
`target_url` is intentional, not an open redirect.

| Status | Item | Where |
|--------|------|-------|
| ✅ | `/help/ask` (public AI endpoint) had no per-IP rate limit — a paid embedding + LLM call per unauthenticated request (denial-of-wallet + DoS for real help users). Added `rateLimitOk('help-ask', clientIp, 10, '60 s')`, matching the other public endpoints. | `app/(help)/help/ask/route.ts` |

## Coverage note

Both fan-outs each had one slice fail its structured-output retries — **API-routes** (pass 1,
re-run above) and **app-wide mobile-perf** (pass 2, closed inline: maps confirmed code-split).
Net of the two re-runs, every planned slice is covered.
