# Maintenance Report — Frequency web — 2026-06-11

> **Status:** Full health sweep after a long admin-dashboard + On Air build session (PRs
> #579–590). **No blockers.** Gates green on the branch (`tsc` 0 · `lint` 0 errors · `vitest`
> 818/818). The "3 failed test files" first seen this session were a container artifact (an
> incomplete `node_modules` missing `stripe` / `@upstash/*`); `pnpm install` fixed it — not a
> code defect. Findings below are quality/canon/DB-hygiene, none urgent.

Legend: ✅ done · ⏳ in progress · ⚠️ needs attention · 🔴 blocker.

## How this sweep was run

Six parallel checks ("bots"): build/lint/typecheck/test, a correctness review of the recent
admin + On Air work, a Page-Framework compliance audit, a voice/naming canon audit of changed
copy, an integration/env-wiring audit, and Supabase security + performance advisors. Prod
Supabase project = `azsqfeonabsbmemvddqd` ("Frequency Community").

## Gate results

| Gate | Result |
| --- | --- |
| `tsc --noEmit` | ✅ 0 errors (after `pnpm install` completed node_modules) |
| `eslint` | ✅ 0 errors · 1 warning (`crew/leaderboard/loading.tsx:3` unused `rank`) |
| `vitest` | ✅ 818 passed / 100 files |
| Open PRs | #591 (draft) — Vercel deploy **Ready**, `mergeable_state: clean`, no human review pending |
| Migrations | ✅ All repo migrations applied to prod (recent ones carry MCP-auto versions — known benign drift) |

## Prioritized to-do (compiled from all six checks)

### P1 — fix soon (correctness · canon · perf)

1. **Voice canon — lowercase "journey" in live copy.** `components/feed/capture-launcher.tsx:117`
   placeholder (`"Share something on your journey…"`) and
   `content/help/getting-started/practices.md:86-87`. NAMING.md: "Journey" is the capitalized
   game object; CONTENT-VOICE §5d bans the vague-noun usage. Fix wording in both.
2. **Page-Framework — admin home blocks the shell on slow awaits.** `app/(main)/admin/page.tsx`
   awaits `getPracticeMetrics()` + a sequential `profiles` count before returning JSX. `growth`
   and `operations` already isolate these behind per-section `<Suspense>` (PAGE-FRAMEWORK §5.3);
   the home page is the outlier. Push the practice read + Pulse ring behind Suspense; fold the
   `profiles` count into the up-top `Promise.all`.
3. **Page-Framework — local `EmptyState` shadows the kit primitive.** `app/(main)/admin/page.tsx:416-431`
   re-implements `EmptyState` with a different prop shape. Delete it; import from
   `@/components/ui/empty-state` and remap call sites (`message`/`cta` → `title`/`action`).

### P2 — cleanup / hygiene

4. **Dead code from the "Mindless" rename.** `components/feed/zap-menu-art.tsx:144` `OnAirArt`
   export now unused; `app/(main)/admin/page.tsx:468` `MEMBERSHIP_SELECT` fetches `volunteer_role`
   that nothing consumes; the lint warning in #1's gate row. All harmless — delete.
5. **aria-label em dash.** `components/layout/app-shell.tsx:971` `aria-label="Zap — capture a moment"`
   — aria-labels are member-facing (screen readers); "no em dashes in brand copy" applies.
   → `"Zap, capture a moment"`.
6. **`.env.example` gaps (silent-prod risk).** Read in code, undocumented: all Stripe vars —
   notably `STRIPE_MEMBERSHIP_AMOUNT` / `STRIPE_SUPPORTER_AMOUNT` (silently default to $10/$25
   if unset → wrong prices), `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_*`, `STRIPE_PLATFORM_FEE_PCT`;
   the Google Wallet trio; `COMPANY_POSTAL_ADDRESS` (CAN-SPAM); `NEXT_PUBLIC_APP_VERSION`.
   Documented-but-dead: `BETA_CONFIRM_SECRET` (zero code refs — remove). All integrations
   degrade gracefully today, so this is documentation, not breakage.

### P3 — database / infra (most are baseline-known)

7. **Auth — enable leaked-password protection.** Single Supabase Auth toggle (1 WARN).
8. **Public buckets allow listing.** `avatars`, `event-media`, `posts` each have a broad public
   SELECT policy permitting object listing (3 WARN). Confirm directory listing is intended.
9. **RLS performance advisors (mechanical, large).** ✅ `auth_rls_initplan` **done** — migration
   `20260615200000_wrap_rls_auth_calls_in_select.sql` wrapped all 63 flagged policies' `auth.*()`
   calls in a scalar subselect (semantics-preserving; applied to prod and verified the advisor
   cleared 63 → 0, dropping the performance sweep from 324 → 243 lints). ⏳ Held for a reviewed
   pass (can change access semantics): 98 × `multiple_permissive_policies` (consolidate overlapping
   policies). Plus 45 unindexed FKs and ~100 unused indexes to review (INFO-level).
10. **Known/benign baseline (no action).** 1 ERROR = `public.spatial_ref_sys` (PostGIS reference
    table, unchanged since 2026-06-02). `security_definer_function_executable` /
    `auth_allow_anonymous_sign_ins` WARNs are standard advisor noise on this schema. Migration
    version drift (repo filenames vs MCP-applied timestamps) is benign — every migration is
    applied.

## Verified non-issues (looked risky, checked clean)

- `not-on-admin-home.tsx` deleted in `28c1770` — zero remaining imports.
- `RingGauge pct={practice.activationRate}` passes a 0..1 fraction; gauge clamps/×100 internally.
- `operations` page `is_cancelled` / `starts_at` filters are real `events` columns.
- On Air `session.tsx` clock/wake-lock/audio-context cleanup + finish guards all correct.
- All Anthropic model refs centralized in `lib/ai/models.ts` (`haiku-4-5`, `sonnet-4-6`,
  `opus-4-8`); one direct `claude-opus-4-8` in `lib/studio/winback.ts` (copilot-gated, intended).

## Out of scope (flagged for a later sweep)

- `content/help/sharing/build-a-journey.md` uses lowercase "journey" pervasively (~15 hits) —
  same canon issue as P1#1 but a separate, larger pass.
