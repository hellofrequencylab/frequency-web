# Maintenance Report — Frequency web — 2026-07-27

> **Status:** Full sweep on the day's third pass (after the meta scan #1962 and the highs
> follow-up #1964). **No blockers.** `pnpm audit`: **12 vulnerabilities → 0** (7 high / 5
> moderate cleared by the Next 16.2.12 patch + two targeted overrides). Migration drift from
> 2026-09-13 forward: **zero in both directions**, re-verified against the live ledger today.
> One draft migration awaits approval for the two advisor WARNs #1961's SMS work introduced.

Legend: ✅ done · ⏳ in progress · ⚠️ needs attention · 🔴 blocker.

## How this sweep was run

The `/maintenance` protocol on branch `claude/site-audit-cleanup-ryuq9g`: Supabase advisors
(security + performance) and ledger-vs-repo drift against the prod project
(`azsqfeonabsbmemvddqd`, "Frequency Community"), `pnpm outdated` + `pnpm audit --prod`,
build/lint/typecheck/test on the bumped tree, and open-PR CI via the GitHub tools. Reads were
automatic; the one prod-touching fix (a migration) is drafted, not applied.

## Gate results

| Gate | Result |
| --- | --- |
| `tsc --noEmit` | ✅ 0 errors, on the bumped dependency tree |
| `eslint` | ✅ 0 errors · 3 warnings, all pre-existing and reproducing on `main` |
| `vitest` | ✅ 576 files / 6458 tests, on the bumped dependency tree |
| `next build` | ⚠️ compile + TypeScript stages ✅ clean on 16.2.12; page-data collection cannot run in this container (no Supabase env — `/discover/cities/[citySlug]` reads the DB at build time). **Pre-existing and environment-only**: the pre-bump tree fails identically here. The **Vercel deploy** is the build gate that has env (CI deliberately does not run `next build` — its header says Vercel owns it); watch the preview deploy on this PR go Ready on the bumped tree. |
| `pnpm audit --prod` | ✅ **0 vulnerabilities** (was 7 high / 5 moderate this morning) |
| 12 machine guards | ✅ all green; `check:migrations` at 549, every version unique |
| Migration drift (2026-09-13 →) | ✅ 166 repo files ⇄ 166 ledger rows, zero in both directions |
| Open PRs | #1964 (draft, this branch) — CI in progress at sweep time; no other open PRs, none stale |

## Supabase — security advisors (193 lints)

| Severity | Finding | Disposition |
| --- | --- | --- |
| ⚠️ ERROR ×1 | `rls_disabled_in_public` on `spatial_ref_sys` | **Not actionable** — PostGIS extension system table; owned by the extension, RLS cannot be enabled on it. Known Supabase limitation ([lint 0013](https://supabase.com/docs/guides/database/database-linter?lint=0013_rls_disabled_in_public)). |
| INFO ×101 | `rls_enabled_no_policy` | **Deliberate** deny-all posture, documented in the 2026-07-27 meta scan. |
| WARN ×52 / ×36 | `security_definer_function_executable` (authenticated / anon) | Bulk are internal helpers already swept by prior lockdown migrations; the three with no internal auth gate remain in **Needs your call** below (unchanged from the meta scan). |
| WARN ×3 | `extension_in_public` | Long-standing (postgis et al.); moving extensions is a project-level decision, not a sweep fix. |

## Supabase — performance advisors (269 lints)

| Severity | Finding | Disposition |
| --- | --- | --- |
| ⚠️ WARN | `auth_rls_initplan` on `sms_consent.sms_consent_select_own` | **NEW since this morning** — arrived with #1961's `sms_groundwork`; calls `auth.uid()` bare, re-evaluated per row. Fix drafted: `20270103000000` ([lint 0003](https://supabase.com/docs/guides/database/database-linter?lint=0003_auth_rls_initplan)). |
| ⚠️ WARN | `multiple_permissive_policies` on `network_contacts` (SELECT × authenticated) | **NEW since this morning** — `network_contacts_select` + `network_contacts_select_shared` both permissive, every row pays both predicates. Consolidation drafted in the same migration; behaviour-preserving because the shared branch is anon-inert ([lint 0006](https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies)). |
| INFO ×247 | `unused_index` | Unchanged; in **Needs your call** (against the 19 unindexed FKs). |
| INFO ×19 | `unindexed_foreign_keys` | Unchanged; same trade-off. |

## Dependencies

**Applied (safe set — patch + minor), verified by build + full suite:**

| Package | From → To | Note |
| --- | --- | --- |
| `next` + `eslint-config-next` | 16.2.10 → **16.2.12** | Patch release clearing **all 12 audit CVEs against Next**: middleware/proxy bypass, two SSRFs, cache confusion ×2, Server Action payload, image DoS, endpoint disclosure. |
| `react` / `react-dom` | 19.2.7 → 19.2.8 | Patch. |
| `@supabase/supabase-js` | 2.110.7 → 2.110.8 | Patch. |
| `@sentry/nextjs` | 10.67.0 → 10.68.0 | Minor. |
| `@tiptap/*` ×5 | 3.28.0 → 3.29.1 | Minor, all five kept in lockstep. |
| `lucide-react` | 1.25.0 → 1.27.0 | Minor. |
| `resend` | 6.17.2 → 6.18.0 | Minor. |
| `@iconify-json/lucide` | 1.2.118 → 1.2.120 | Patch, dev. |

**Overrides (transitives the direct bumps could not reach):**

| Override | Why |
| --- | --- |
| `postcss: 8.5.23` | The existing pin (`8.5.16`, arrived unexplained in #1911) was itself the vulnerable version — path traversal, patched ≥ 8.5.18. Pin kept, moved to current patch. |
| `sharp: >=0.35.0` | Inherited libvips CVEs (high), patched ≥ 0.35.0; Next 16.2.12 still ships 0.34.5. |
| `brace-expansion@>=5.0.0 <5.0.8: >=5.0.8` | DoS via unbounded expansion (high). Scoped to the 5.x consumers only, so the 1.x line other tooling depends on is untouched. |

**Held back (majors — never auto-bumped, per this file's contract):**

| Package | Current → Latest | Risk note |
| --- | --- | --- |
| `typescript` | 6.0.3 → 7.0.2 | Compiler major; touches every file. Bump alone in a dedicated PR. |
| `eslint` | 9.39.4 → 10.8.0 | Major; flat-config and rule changes ripple into `eslint-config-next` compatibility. |
| `maplibre-gl` | 5.24.0 → 6.0.0 | Major; the map surfaces (events map lane, nodes) need a visual pass. |
| `jsdom` | 29.1.1 → 30.0.0 | Test-env major; cheap to try but belongs with a test-infra PR, not a security sweep. |
| `@anthropic-ai/sdk` | 0.112.3 → 0.115.0 | 0.x line, so minors may break; it feeds Vera. Bump with a Vera smoke test. |

## Drift verification (the "no drifts after this" check)

| Surface | State |
| --- | --- |
| Repo migrations ⇄ prod ledger (2026-09-13 →) | ✅ 166 ⇄ 166, zero drift either direction, checked live today |
| `20270102000000` (#1961's migration) | ✅ was applied-but-unrecorded; verified applied against the live catalog, recorded this morning |
| `schema_migrations` versions | ✅ 549 in repo, every version unique (`check:migrations`, now in CI) |
| Docs ⇄ decisions | ✅ ADR-855 recorded with the JSON-LD fix; META-SCAN-STATUS.md updated same-commit |
| Ledger before 2026-09-13 | ⚠️ 477 historical rows with no repo file — documented divergence, cosmetic; in **Needs your call** |

## Needs your call

1. **Apply the drafted advisor migration** `20270103000000_advisor_sweep_sms_initplan_contacts_policies.sql`
   (in this PR). Not applied, per the sweep contract. Exact step after merge: apply via the
   Supabase MCP (`apply_migration` with the file's name and contents), which also records the
   ledger row — or `supabase migration up` from a machine with the service key. Clears both
   WARNs; behaviour-preserving (rationale in the file header).
2. **The recurring ledger-drift class.** `check:migrations` cannot see "applied but never
   recorded" (a DB fact, not a repo fact). Wiring the ledger⇄repo comparison into this
   `/maintenance` skill closes it permanently — small change, needs your OK since the skill is
   an operator contract.
3. **Unchanged from the meta scan:** three anon-callable SECURITY DEFINER RPCs
   (`circle_momentum`, `resonance_neighbors`, `mkt_interest_demand`), 19 unindexed FKs vs 247
   unused indexes, the duplicate `record_qr_scan` overload, and the pre-09-13 ledger history.

## What was deliberately not done

- No majors bumped (contract above). No DDL executed. No PR merged.
- `@anthropic-ai/sdk` left at 0.112.3 — it feeds Vera and 0.x minors can break.
- The 101 deny-all RLS INFOs and the `spatial_ref_sys` ERROR are recorded as not-actionable,
  not silently dropped.
