# Maintenance Report — Frequency web — 2026-06-02

> **Status:** automated advisor sweep on project `azsqfeonabsbmemvddqd`
> ("Frequency Community"). ✅ 46 FK indexes + `set_updated_at` search-path **applied to
> prod** (2026-06-02; FK gaps now 0) · ⚠️ items await your call · 🔴 1 ERROR (benign
> PostGIS `spatial_ref_sys`). Draft-and-approve.

Legend: ✅ done · ⏳ in progress · ⚠️ needs attention · 🔴 blocker.

## Supabase — Security (113 findings)
| Sev | Finding | Detail | Fix |
| --- | --- | --- | --- |
| ERROR | `rls_disabled_in_public` (`spatial_ref_sys`) | PostGIS system table is public without RLS | **Benign/known** — PostGIS reference table, read-only. Acknowledge; can't enable RLS on an extension-owned table. |
| WARN | `security_definer_function_executable` ×82 | many `SECURITY DEFINER` fns callable via RPC by anon/authenticated | ⚠️ **Not a blanket revoke** (verified 2026-06-02): fns used inside RLS need EXECUTE to evaluate, and intended RPCs like `my_notifications` need EXECUTE by `authenticated`. Per-function review — relocate truly-internal ones to a `private` schema; keep intended RPCs. None called as `.rpc()` for the 4 shared helpers. |
| ✅ / WARN | `function_search_path_mutable` ×15 | mutable `search_path` | **`set_updated_at` applied 2026-06-02** (body only calls `now()` → safe). The other 14 reference tables/other schemas — pin `search_path` per-function only after checking each body. |
| WARN | `public_bucket_allows_listing` ×2 | broad SELECT lets clients list bucket files | Narrow the storage SELECT policy |
| WARN/INFO | `extension_in_public` ×2 | PostGIS extensions in `public` | Known (PostGIS); move to a dedicated schema only if convenient |
| INFO | `rls_enabled_no_policy` ×10 | `agent_actions`, `automation_rules`, `campaigns`, `contacts`, `email_events`, `email_suppressions`, `nodes`, `notification_queue`, `pages`, `team_members` — RLS on, no policy = service-role-only | **Confirm each is intentionally backend-only.** If any is meant to be client-readable, it currently denies all access. |

[Linter reference](https://supabase.com/docs/guides/database/database-linter)

## Supabase — Performance (227 findings)
| Count | Finding | Recommended fix |
| --- | --- | --- |
| 92 | `multiple_permissive_policies` | Consolidate overlapping permissive policies (review-heavy) |
| 48 | `unused_index` | Drop unused indexes (verify against query patterns) |
| ✅ 0 | `unindexed_foreign_keys` | **Cleared** (was 46) — covering indexes applied to prod 2026-06-02 |
| 41 | `auth_rls_initplan` | **High-value, mechanical:** wrap `auth.uid()`/`auth.role()` as `(select auth.uid())` in policies |

## Highest-priority queue
1. **Verify the 10 `rls_enabled_no_policy` tables are intentionally backend-only** — these
   hold CRM/automation/email data; a missing policy on a table meant to be client-readable
   is a silent access bug. (Most look service-role-only by design — confirm.)
2. **`auth_rls_initplan` ×41 + `unindexed_foreign_keys` ×46** — biggest mechanical perf win;
   bundle into one migration.
3. Revoke RPC EXECUTE on non-public SECURITY DEFINER functions.

## ✅ Applied — FK indexes migration
`supabase/migrations/20260602195255_fk_indexes.sql` — **46 FK covering indexes applied to
prod 2026-06-02** (purely additive, `IF NOT EXISTS`). Recorded as ADR-058. Clears the
`unindexed_foreign_keys` category. Repo migration matches prod.

`supabase/migrations/20260602210000_set_updated_at_search_path.sql` — **`set_updated_at`
search_path pinned to `''`, applied to prod 2026-06-02** (body only calls `now()` → safe).
Clears one of the 15 `function_search_path_mutable` findings.

## Needs your call (NOT in the migration — these change behaviour)
1. **Verify the 10 `rls_enabled_no_policy` tables** are intentionally backend/service-role-only.
2. **RLS init-plan rewrites ×41** — wrap `auth.uid()` as `(select auth.uid())`; I can draft a
   follow-up migration on confirmation.
3. **SECURITY DEFINER RPC exposure ×82** — ⚠️ **not** a blanket `REVOKE EXECUTE` (verified
   2026-06-02 it would break RLS for fns used in policies, and break intended RPCs like
   `my_notifications`). Per-function: relocate truly-internal helpers to a `private` schema,
   keep intended RPCs. Needs a review pass.
4. Acknowledge the `spatial_ref_sys` ERROR as known-PostGIS (no action).
