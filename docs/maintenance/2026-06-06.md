# Maintenance Report — Frequency web — 2026-06-06

> **Status:** Stage B close-out (Local Marketplace round-out + Density read-model) verified
> live on project `azsqfeonabsbmemvddqd` ("Frequency Community"). ✅ both migrations applied
> to prod · ✅ a reversible demo network seeded · ⚠️ migration version drift noted (benign) ·
> 🔴 1 ERROR (benign PostGIS `spatial_ref_sys`, unchanged).

Legend: ✅ done · ⏳ in progress · ⚠️ needs attention · 🔴 blocker.

## Shipped this session

| Change | PR | ADR | Migration |
| --- | --- | --- | --- |
| Local Marketplace round-out (near-me sort, listing edit, image galleries) | #347 | ADR-148 | — (UI/lib only) |
| Density / demand read-model (`/admin/expansion`) | #352 | ADR-151 | `density_demand_read_model` |
| Demo generator tags members with their circle's city + home coords | #352 | ADR-151 | — |

Both PRs are **merged to `main`**. Gates green on `main`: `tsc`, `lint`, `vitest` (349 tests).

## Supabase — advisors (security)

- Re-ran the security advisor sweep. The new `density_by_city()` function raises **no
  finding** — it is `SECURITY DEFINER` with a pinned `search_path` and `EXECUTE` revoked from
  `public`/`anon`/`authenticated` (granted to `service_role` only).
- 🔴 The single `rls_disabled_in_public` ERROR is still `public.spatial_ref_sys` — the
  PostGIS reference table. **Benign/known** (extension-owned, read-only); unchanged from the
  2026-06-02 sweep.
- No new RLS gaps from this session: `market_listings` carries its RLS (ADR-148); density is
  a function, not a table.

## ⚠️ Migration version drift (repo ↔ applied)

These two migrations were applied via the Supabase MCP, which recorded **auto-generated
versions** that differ from the repo filenames:

| Repo file | Applied version |
| --- | --- |
| `20260607090000_market_listings.sql` | `20260606032345` |
| `20260608000000_density_demand_read_model.sql` | `20260606040502` |

Impact is low: both migration bodies are **idempotent** (`create table` is the only
non-idempotent one — `market_listings` already exists, so a replay would no-op or be
skipped by the tracker; `density_by_city` is `create or replace function`). A future
`supabase db pull`/repair can reconcile the version strings if desired. No action required
to keep prod working.

## ✅ Demo network seeded (reversible)

To make the now-empty surfaces demonstrable (Expansion, Marketplace, Circles, maps), a demo
network was seeded across five North-County cities. All rows are `is_demo` and
`demo_mode` is on, so the whole layer toggles/purges from **Admin → Demo Studio**.

| City | Circles | Fill | Residents | Stage |
| --- | --- | --- | --- | --- |
| Encinitas | 2 | 90% | 65 | ✅ Ready (⚠️ at capacity) |
| Carlsbad | 2 | 40% | 40 | ⏳ Growing |
| Oceanside | 1 | 30% | 36 | ⏳ Growing |
| Vista | 0 | — | 18 | 🌱 Seed |
| Cardiff | 0 | — | 7 | 🌱 Seed |

Totals: **165** `is_demo` profiles · **5** circles · **87** memberships · **12** listings ·
**5** posts. `circles.member_count` is trigger-maintained (real membership rows). Member
home coordinates and `city` follow their circle's city (the generator change above), so the
Expansion read-model counts them as residents and the map plots them correctly.

To remove: **Admin → Demo Studio → purge** (or toggle demo mode off to hide).
