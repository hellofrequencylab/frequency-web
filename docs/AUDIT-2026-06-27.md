# Site-wide audit — 2026-06-27

A multi-agent sweep (security · SEO/AIO · performance · leftovers) plus the owner's concrete
bug list. Legend: ✅ fixed this sweep · 📋 tracked follow-up.

## Owner bug list — all fixed (separate PRs)
| Bug | Fix | PR |
|---|---|---|
| Self-comment/react/RSVP farms Zaps/Gems/badges | Server-side `author_id !== actor` self-exclusion + first-time/dedupe gating on every feed/social reward route; reply-count migration excludes self-replies | #1154 ✅ |
| Zaps/Gems not showing on profiles | Standing card (Zaps·Gems·Streak·Rank) now renders on every profile (was paid-tier-gated) | #1153 ✅ |
| No awards / shop items on profiles | New Awards + Collection section from `user_achievements` + `store_redemptions` | #1153 ✅ |
| Gem totals stale after spending | Store redeem/gift now revalidate the root layout (the balance shows in the global shell header) | #1153 ✅ |
| Practices not linked to the timer (logged in one tap) | `logPractice` refuses a one-tap on a timed practice; one-tap button removed for timed practices | #1156 ✅ |
| Timer not auto-configured | Opens to the practice's `timer_kind` + `duration_min` | #1156 ✅ |
| Timer doesn't auto-start on select | `autoStart` threaded through the Mindless/Movement sessions | #1156 ✅ |
| Daily Walk loggable without a timer | Migration reclassifies `daily-walk`/`morning-movement` to `timer_kind='movement'` | #1156 ✅ |
| iOS PWA top nav under the notch + landscape glitch | `env(safe-area-inset-top)` on all top bars + `min-h-dvh` + side insets | #1155 ✅ |

## Migrations to APPLY on merge (written, not pushed — per CLAUDE.md)
- `20260627000000_reply_count_exclude_self.sql` (#1154) — reply_count excludes self-replies + backfill.
- `20260825000000_daily_walk_timed.sql` (#1156) — reclassify daily-walk/morning-movement as timed.

## SEO / AIO — ✅ fixed this sweep (PR: SEO fixes)
- `/spotlight/[handle]` now emits `alternates.canonical`, `openGraph.type=profile` + url, and `personSchema` + `breadcrumbSchema` JSON-LD.
- Published Spotlight profiles added to `app/sitemap.ts` (admin-read of the published flag; only the handle crosses out).
- `/privacy` + `/terms` gained `openGraph`.
- 📋 Follow-up: a dedicated `app/spotlight/[handle]/opengraph-image.tsx` for branded share cards (P2).
- Confirmed strong baseline: dynamic sitemap (parallel, redaction-safe, image entries), AI-bot-aware robots (11 named bots), comprehensive JSON-LD library, `llms.txt` + `llms-full.txt`, per-route OG images for events/partners/practices/pillars/spaces/help.

## Performance — 📋 tracked follow-ups (no correctness issue; throughput)
- **P0 — Profile page** (`app/(main)/people/[handle]/page.tsx`): ~8–10 sequential awaits on the critical path (header re-read, payouts+connect, 3 capability/role reads, friendship, block, linked-contact) that are mutually independent. Fix: collapse into one `Promise.all`, fold the header re-read into the first select, and push the post timeline behind `<Suspense>` so the identity band paints first.
- **P1 — Events index** (`app/(main)/events/index-data.ts`): long serial auth→profile→memberships→content→flags chain; the two `event_rsvps` reads should be one `Promise.all`. Mirror the well-parallelized circles loader.
- **P1 — Feed page** (`app/(main)/feed/page.tsx`): `getLocalActivity` should join the existing `Promise.all`; `getMemberPillarBalance` should move into the streamed `JourneyBoard`.
- **P2 — Discover** (`app/discover/page.tsx` + `lib/discover.ts`): `getPublicCircles` runs 3× (6, 200, 200-in-clusters). Fetch once + slice, or `React.cache` the public RPCs.
- **P2 — Images**: convert the two large hot `<img>` (spotlight header, sequence-wizard cover) to `next/image`.
- **Clean**: feed/circles/discover already use `Promise.all` + `<Suspense>`; all hot RPCs limit-bounded; maps + Puck editor dynamically imported / RSC; no framer-motion/recharts bundle offenders.

## Security — beyond the farming fix
- `check-authz-guards.mjs` passes across all PRs. The farming exploit (the real P0) is closed in #1154.
- 📋 Follow-up: run `get_advisors(security)` review on the prod DB for RLS coverage (the advisor output was too large to read inline this pass; a targeted `pg_policies` sweep is the next step).

## Build queue (added)
Spotlight MySpace/Discord socials: **Top Friends** (build first), **Guestbook**, **stickers** — see `BUILD-LIST.md`.
