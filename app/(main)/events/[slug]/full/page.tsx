// Signed-in members land here via proxy rewrite (lib/nav/member-event-rewrite.ts).
// The browser URL stays /events/:slug; x-pathname stays the share path.
export { default, generateMetadata } from '../event-member-page'
