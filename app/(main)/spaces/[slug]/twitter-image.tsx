// X/Twitter share card for a Space — the SAME hero card as the OG image (one source, re-exported,
// the site-root pattern), so every network previews the identical hero composition.
export { default, alt, size, contentType } from './opengraph-image'
